[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "restmcp",
]

[tool.pytest.ini_options]
pythonpath = [".."]
